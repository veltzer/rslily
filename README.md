# rslily

A Rust take on LilyPond music engraving.

This crate is at an early stage: the project skeleton, CI and release
tooling are in place but the engraving itself is not implemented yet.

Documentation lives in the `docs/` mdBook.
